using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace KWY
{
    public enum PlayerSkill : int
    {
        // temp
        LightingVolt = 1,
    }

    public enum AID : int
    {
        // temp
        DoubleMove = 1,

    }
    public enum CID : int
    {
        // temp
        Flappy = 1,
    }

    public enum SID : int
    {
        FireBall = 1,
    }

    public enum BID : int
    {
        Burn = 1,
    }

}
