using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace KWY
{

    public enum AID : int
    {
        // temp
        DoubleMove = 1,

    }
    public enum CID : int
    {
        // temp
        Flappy = 1,
    }

    public enum SID : int
    {
        FireBall = 1,
        KnightNA = 2,
        KnightSA = 3,
    }

    public enum BID : int
    {
        Burn = 1,
    }

    public enum PSID : int
    {
        Flash = 1,
    }
}
